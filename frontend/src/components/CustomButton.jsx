import React from 'react';

const CustomButton = () => {
    return (
        <div>
            <button type="button" className='button'>GENERATE TESTS</button>
        </div>
    );
};

export default CustomButton;